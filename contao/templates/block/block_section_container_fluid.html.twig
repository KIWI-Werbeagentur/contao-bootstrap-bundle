{# Basic section with a full-width Bootstrap container-fluid #}
<div id="{{ id }}" class="{{ id }}">
    <div class="{{ id }}__container custom-container container-fluid">
        <div class="{{ id }}__inside inside">
            <div class="{{ id }}__content row">
                {{ content|raw }}
            </div>
        </div>
    </div>
</div>
