{# Basic section with a fixed-width Bootstrap container #}
<div id="{{ id }}" class="{{ id }}">
    <div class="{{ id }}__container custom-container container">
        <div class="{{ id }}__inside inside">
            <div class="{{ id }}__content row">
                {{ content|raw }}
            </div>
        </div>
    </div>
</div>
